<h3><%= t('.title') %></h3>
<%= form_with url: password_resets_path do |f| %>
    <%= f.label :email %>
    <%= f.email_field :email, class:"input input-bordered" %>
    <%= f.submit t('helpers.submit.send'), class:"btn" %>
<% end %>
