<div class="flex flex-col items-center pt-4 gap-3">
    <h1 class="text-xl font-bold underline decoration-2 underline-offset-2"><%= t('.title') %></h1>
    <%= render "form", user: @user %>
    <div class="flex flex-col gap-2">
        <%= link_to t('.create_with_line'), auth_at_provider_path(provider: :line), class: "btn btn-sm bg-green-500" %>
        <%= link_to t('.to_login_page'), login_path, class: "btn btn-sm" %>
    </div>
</div>
