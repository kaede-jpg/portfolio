<h1><%= t('.title') %></h1>
<%= render @user %>
<div>
  <%= link_to t('.edit_user'), edit_user_path(@user), class: "btn" %> |
  <%= link_to "To users", users_path, class: "btn" %>
  <%= button_to t('helpers.submit.delete'), @user, method: :delete, class: "btn" %>
</div>
