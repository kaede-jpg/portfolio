<dialog id="new_record_modal" class="modal">
  <div class="modal-box">
    <form method="dialog">
      <button class="btn btn-sm btn-circle btn-ghost absolute right-2 top-2">✕</button>
    </form>
    <div class="flex flex-col items-center pt-4 gap-3">
      <h3 class="font-bold text-lg"><%= t('.create_record') %></h3>
      <%= render "form", record: record %>
    </div>
  </div>
</dialog>