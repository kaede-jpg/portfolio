<!DOCTYPE html>
<html  data-theme="cupcake">
  <head>
    <title>ぷに活監視システム</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body>
    <header class="fixed top-0 left-0 right-0 z-50">
      <% if logged_in?%>
        <%= render 'shared/header'%>
      <% else %>
        <%= render 'shared/before_login_header'%>
      <% end %>
    </header>
    <div class="mt-16">
      <% if flash[:notice] %>
        <p id="notice" class="alert alert-success sm-0">
          <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
          <%= flash[:notice] %>
        </p>
      <% end %>
      <% if flash[:alert] %>
        <p id="alert" class="alert alert-error">
          <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
          <%= flash[:alert] %>
        </p>
      <% end %>
    </div>
    <div class="relative px-4">
      <div class="loader fixed top-0 bottom-0 left-0 right-0 z-50 flex items-center justify-center">
        <div class="rounded-lg bg-neutral w-20 h-20 flex items-center justify-center">
          <div class="loading loading-spinner loading-lg"></div>
        </div>
      </div>
      <%= yield %>
    </div>
  </body>
</html>
